bài tập lớn môn NLP.
XXây dựng mô hình dịch máy bằng kiên trúc Transformer, (code from scratch).

file trọng số và data để train ở trong folder data và weights ko đẩy lên vì to quá 

Chạy file translate.py để dùng

Mọi người muốn dùng thì clone repo, sau đó tải file trọng số ở link Google Drive trong thư mục `weights`.
Sau khi giải nén, cấu trúc thư mục như sau:

```text
project/
└── weights/
    ├── fine_tune.pt
    ├── iwslt_best.pt
    └── vlsp_best.pt


